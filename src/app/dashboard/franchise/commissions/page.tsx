import AdminCommissionsPage from '../../admin/commissions/page';

export default function FranchiseCommissionsPage() {
    return <AdminCommissionsPage />; // Resuing component; we could add auth scope filtering in real app, but backend handles filtering.
}
